Explique a seguinte afirmação: "Um Modelo de refletir o seu aspecto temporal".

Um modelo de banco de dados que apenas armazena os valores atuais de informação é diferente do de modelo banco de dados que armazena o histórico da informação. Logo, é importante considerar o aspecto temporal na modelagem dos dados.
